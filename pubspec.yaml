name: horloge_project
description: A new Flutter project.

publish_to: 'none'


version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  spa:
    git:
      url: https://github.com/JmIsk/dart-spa

  cupertino_icons: ^1.0.2
  solar_calculator: ^1.0.2
  sunrise_sunset: ^1.0.4
  stop_watch_timer: ^1.2.0+1
  daylight: ^2.0.0
  intl: ^0.17.0
  #spa: ^1.1.0
  circular_countdown_timer: ^0.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true

